# General
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editors
.idea/
.vscode/
*.swp
*.swo

# Dependencies
node_modules/
package-lock.json # Keep this tracked usually, but sometimes people ignore it. Standard is to TRACK it. I will keep it tracked.
yarn.lock         # Keep tracked.

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native Configs (Expo Managed)
# If you are using "prebuild" (Expo CNG) and want to generate native folders on demand, keep these ignored.
# If you have custom native code, uncomment these lines.
/ios/
/android/

# Build outputs
*.apk
*.aab
*.ipa

# Keys and Secrets
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision
*.pem

# Environment Variables
.env
.env.local
.env.*.local
*.env
*.env.*
# Exception: Always commit the example file
!.env.example

# Testing
coverage/

# Bundlers
.metro-health-check*
